RegularExpressionVisualizer
===========================

Just a little assignment from Programming languages, parses regular expressions from the command line and creates dot files which can be converted to images
